set(
    COMPONENT_SRCS 
        "firestore_utils.cc"
        "firebase_auth.cc"
        "firebase_common.cc"
    )

set(
    COMPONENT_REQUIRES 
        "esp_http_client" 
        "freertos" 
        "log"
        "esp-tls"
        "json"
    )

set(COMPONENT_ADD_INCLUDEDIRS "." )  # For all the .h files

set(
    COMPONENT_EMBED_FILES
        "googleapis_com_chain.pem" 
        "website_certificate/secure_token_googleapis_chain.pem"
    )

register_component()